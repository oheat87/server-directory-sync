#include <stdio.h>

int main(){
    // 디렉토리 파일 동기화 프로그램의 흐름
    /*
        1. 두 서버에 연결
        2. 서버의 각 디렉토리로 이동
        3. 두 디렉토리에서 파일 하나씩 비교
        4. 만약 서로 다른 파일 있으면 더 최신의 파일로 동기화

        -생각해볼 부분
        >> time interval 마다 파일을 비교해야 하는데, 이것을 어떻게 구현할지
        >> 두 파일이 다르다는 것을 무엇을 기준으로 확인할지)
        >> 파일 동기화에 있어서 다른 생각해봐야 할 이슈는 없는지

        
        (이 프로그램과는 별개로)
        ++ CTO님께서 말씀하신 프로세스 하나를 추적해서 만약 프로세스 실행이 안되고 있으면
            다시 실행시켜주는 프로그램도 짜야 합니다...
    */
    
    return 0;
}